const APP_BUILD='2026-09-18-05';
const leagueKey='afrn_leagues';
const followedKey='afrn_followed_teams';const followedPlayersKey='afrn_followed_players';const followedCompetitionsKey='afrn_followed_competitions';const favouritesOnboardedKey='afrn_favourites_onboarded';let followTab='competitions';let followCache={teams:[],players:[],competitions:[]};function getFollowed(){try{return JSON.parse(localStorage.getItem(followedKey)||'[]')}catch(e){return[]}}function getFollowedPlayers(){try{return JSON.parse(localStorage.getItem(followedPlayersKey)||'[]')}catch(e){return[]}}function getFollowedCompetitions(){try{return JSON.parse(localStorage.getItem(followedCompetitionsKey)||'[]')}catch(e){return[]}}function authSession(){try{return JSON.parse(localStorage.getItem('afrn_auth_session')||'null')}catch(e){return null}}async function saveCloudPreferences(){const s=authSession();if(!s?.access_token||!s?.user?.id)return;try{await fetch(SUPABASE_URL+'/rest/v1/user_football_preferences?on_conflict=user_id',{method:'POST',headers:{apikey:SUPABASE_KEY,Authorization:'Bearer '+s.access_token,'Content-Type':'application/json',Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify({user_id:s.user.id,followed_teams:getFollowed(),followed_players:getFollowedPlayers(),followed_competitions:getFollowedCompetitions(),updated_at:new Date().toISOString()})})}catch(e){console.warn('Preference sync failed',e)}}async function loadCloudPreferences(){const s=authSession();if(!s?.access_token||!s?.user?.id)return;try{const r=await fetch(SUPABASE_URL+'/rest/v1/user_football_preferences?user_id=eq.'+encodeURIComponent(s.user.id),{headers:{apikey:SUPABASE_KEY,Authorization:'Bearer '+s.access_token}});if(!r.ok)return;const rows=await r.json();if(rows[0]){localStorage.setItem(followedKey,JSON.stringify(rows[0].followed_teams||[]));localStorage.setItem(followedPlayersKey,JSON.stringify(rows[0].followed_players||[]));localStorage.setItem(followedCompetitionsKey,JSON.stringify(rows[0].followed_competitions||[]))}}catch(e){console.warn('Cloud preferences unavailable',e)}}function toggleFollow(id){const a=getFollowed(),k=String(id),i=a.indexOf(k);i>=0?a.splice(i,1):a.push(k);localStorage.setItem(followedKey,JSON.stringify(a));saveCloudPreferences();render();renderFollowedPreview();renderFollowList()}function toggleFollowPlayer(id){const a=getFollowedPlayers(),k=String(id),i=a.indexOf(k);i>=0?a.splice(i,1):a.push(k);localStorage.setItem(followedPlayersKey,JSON.stringify(a));saveCloudPreferences();renderFollowedPreview();renderFollowList()}function toggleFollowCompetition(id){const a=getFollowedCompetitions(),k=String(id),i=a.indexOf(k);i>=0?a.splice(i,1):a.push(k);localStorage.setItem(followedCompetitionsKey,JSON.stringify(a));saveCloudPreferences();renderFollowedPreview();renderFollowList()}

const teamKey='afrn_master_teams';
const SUPABASE_URL='https://jjqhvruppafpumcthmwe.supabase.co';
const SUPABASE_KEY='sb_publishable_02hhRG8bgDOqSFxva8IMvQ_zWTLMa3G';
const defaults=[{name:'Ligi Kuu',type:'League',division:'Ligi Kuu',season:'2026/2027',teams:[]},{name:'Daraja la I',type:'League',division:'Daraja la I',season:'2026/2027',teams:[]},{name:'Daraja la II',type:'League',division:'Daraja la II',season:'2026/2027',teams:[]},{name:'Daraja la III',type:'League',division:'Daraja la III',season:'2026/2027',teams:[]},{name:'UPENDO WA WAKIMBIZI CUP',type:'Tournament',division:'Daraja la I',season:'2026',teams:[],source:'supabase',sourceCompetitionId:'3b55884e-05bc-4d83-832e-3addcd97d970'}];
function getLeagues(){const x=localStorage.getItem(leagueKey);if(!x){localStorage.setItem(leagueKey,JSON.stringify(defaults));return defaults}const a=JSON.parse(x);if(!a.some(l=>String(l.name).toLowerCase()==='upendo wa wakimbizi cup')){a.push(defaults[4]);localStorage.setItem(leagueKey,JSON.stringify(a))}return a}
function getTeams(){const x=localStorage.getItem(teamKey);return x?JSON.parse(x):[]}
function saveTeams(x){localStorage.setItem(teamKey,JSON.stringify(x))}
function divisionOf(l){return l.division||l.name}
function teamsForLeague(l){return getTeams().filter(t=>divisionOf(l)==='Open'||t.division===divisionOf(l))}
function esc(s){return String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]))}
function render(){
  const box=document.getElementById('leagues');if(!box)return;
  const all=getLeagues();
  const divs=all.filter(l=>l.type!=='Tournament'&&/^(Ligi Kuu|Daraja la I|Daraja la II|Daraja la III|Daraja la IV)$/i.test(String(l.name||'')));
  const cups=all.filter(l=>l.type==='Tournament');
  const otherLeagues=all.filter(l=>l.type!=='Tournament'&&!/^(Ligi Kuu|Daraja la I|Daraja la II|Daraja la III|Daraja la IV)$/i.test(String(l.name||'')));
  let html='';
  if(divs.length){
    const defaultLeague=divs.find(l=>String(l.name).toLowerCase()==='ligi kuu')||divs[0];
    html+=`<article class="competition-card"><div class="comp-head"><div><div class="comp-title">⚽ Ligi Kuu</div><div class="comp-meta">${esc(defaultLeague.season||'2026/2027')} • Chagua Daraja</div></div><span class="badge">LIGA</span></div><div class="division-picker"><label for="mainDivision">Daraja</label><select id="mainDivision">${divs.map((l,i)=>`<option value="${esc(l.sourceCompetitionId||l.id||i)}">${esc(l.name)}</option>`).join('')}</select></div><div class="score-row"><div><b id="mainLeagueTeamCount">${esc(defaultLeague.teamCount??teamsForLeague(defaultLeague).length)}</b><div class="status">Timu</div></div><div class="score">—</div><div><b id="mainLeagueMatchCount">${esc(defaultLeague.matchCount??'—')}</b><div class="status">Mechi</div></div></div><div class="modules"><div class="module">📅 Ratiba</div><div class="module">🏁 Matokeo</div><div class="module">📊 Msimamo</div><div class="module">⚽ Wafungaji</div><div class="module">🟨 Kadi</div><div class="module">📈 Stats</div><div class="module">📚 Historia</div><div class="module">⚖️ Nidhamu</div></div><button class="open" id="openMainLeague">Fungua Ligi →</button></article>`;
  }
  html+=cups.map((l,i)=>{
    const openId=l.sourceCompetitionId||l.id||i;
    return `<article class="competition-card" onclick="openLeague('${openId}')"><div class="comp-head"><div><div class="comp-title">🏆 ${esc(l.name)}</div><div class="comp-meta">${esc(l.season||'')} • Mashindano/Cup</div></div><span class="badge">CUP</span></div><div class="score-row"><div><b>${esc(l.teamCount??0)}</b><div class="status">Timu</div></div><div class="score">${esc(l.matchCount??'—')}</div><div><b>${esc(l.matchCount??0)}</b><div class="status">Mechi</div></div></div><div class="cup-flow">Makundi A–F • Knockout</div><button class="open">Fungua Mashindano →</button></article>`
  }).join('');
  html+=otherLeagues.map((l,i)=>{
    const openId=l.sourceCompetitionId||l.id||i;
    return `<article class="competition-card" onclick="openLeague('${openId}')"><div class="comp-head"><div><div class="comp-title">⚽ ${esc(l.name)}</div><div class="comp-meta">${esc(l.season||'')} • Ligi</div></div><span class="badge">LIGA</span></div><div class="score-row"><div><b>${esc(l.teamCount??teamsForLeague(l).length)}</b><div class="status">Timu</div></div><div class="score">${esc(l.matchCount??'—')}</div><div><b>${esc(l.matchCount??0)}</b><div class="status">Mechi</div></div></div><button class="open">Fungua Ligi →</button></article>`
  }).join('');
  box.innerHTML=html;
  const sel=document.getElementById('mainDivision'),btn=document.getElementById('openMainLeague'),count=document.getElementById('mainLeagueTeamCount'),matchCount=document.getElementById('mainLeagueMatchCount');
  const update=()=>{
    const l=divs.find(x=>String(x.sourceCompetitionId||x.id||'')===String(sel?.value))||divs[0];
    if(count)count.textContent=String(l?.teamCount??teamsForLeague(l||{}).length??0);
    if(matchCount)matchCount.textContent=String(l?.matchCount??'—');
  };
  if(sel&&btn){sel.onchange=update;btn.onclick=()=>{if(sel.value)openLeague(sel.value)};update()}
}
function openLeague(id){location.href='competition.html?id='+encodeURIComponent(id)}
function setupStatsSelector(){const select=document.getElementById('statsCompetition'),button=document.getElementById('openSelectedStats');if(!select||!button)return;const leagues=getLeagues();select.innerHTML='<option value="">— Chagua ligi —</option>'+leagues.map((l,i)=>`<option value="${esc(l.sourceCompetitionId||l.id||i)}">${esc(l.name)} • ${esc(l.season||'')}</option>`).join('');select.onchange=()=>{button.disabled=select.value==='';if(select.value!=='')loadHomeStats(leagues[Number(select.value)]);else showHomeStatsMessage('Chagua ligi ili kuona takwimu zake hapa.')};button.onclick=()=>{if(select.value!=='')location.href=`competition.html?id=${encodeURIComponent(select.value)}#stats`}}

function statsCss(){if(document.getElementById('homeStatsCss'))return;const s=document.createElement('style');s.id='homeStatsCss';s.textContent='.home-stats{margin-top:12px}.home-stat-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.home-stat-card{border:1px solid #e1e6ee;border-radius:12px;background:#fff;padding:12px}.home-stat-title{font-size:11px;font-weight:900;color:#697487;text-transform:uppercase;margin-bottom:9px}.home-stat-values{display:grid;grid-template-columns:1fr 45px 1fr;align-items:center;text-align:center;gap:5px}.home-stat-values b{font-size:16px}.home-stat-values span{display:block;font-size:10px;color:#8791a0}.home-stat-bar{height:6px;background:#edf0f3;border-radius:99px;overflow:hidden;display:flex;margin-top:9px}.home-stat-bar i{height:100%;display:block}.home-stat-bar i:first-child{background:#1473e6}.home-stat-bar i:last-child{background:#7c8797}.home-stat-note{font-size:12px;color:#697487;margin:8px 0}.home-stat-table{margin-top:12px;overflow-x:auto;border:1px solid #e1e6ee;border-radius:10px}.home-stat-table table{min-width:420px}@media(max-width:650px){.home-stat-grid{grid-template-columns:1fr}}';document.head.appendChild(s)}
function showHomeStatsMessage(msg){const h=document.getElementById('homeStats');if(h)h.innerHTML='<div class="empty">'+esc(msg)+'</div>'}
function statValue(rows,side,key,mode){const vals=rows.map(r=>Number(r[side+'_'+key])).filter(Number.isFinite);if(!vals.length)return null;if(mode==='avg')return vals.reduce((a,b)=>a+b,0)/vals.length;return vals.reduce((a,b)=>a+b,0)}
function statCard(label,h,a,d=0,pct=false){if(h==null&&a==null)return '';const total=(h||0)+(a||0),hp=total?(h||0)/total*100:50;return '<div class="home-stat-card"><div class="home-stat-title">'+label+'</div><div class="home-stat-values"><div><b>'+(h==null?'—':h.toFixed(d))+(pct&&h!=null?'%':'')+'</b><span>NYUMBANI</span></div><strong>VS</strong><div><b>'+(a==null?'—':a.toFixed(d))+(pct&&a!=null?'%':'')+'</b><span>UGENINI</span></div></div>'+(total?'<div class="home-stat-bar"><i style="width:'+hp+'%"></i><i style="width:'+(100-hp)+'%"></i></div>':'')+'</div>'}
async function findCompetitionId(l){if(l?.sourceCompetitionId)return l.sourceCompetitionId;const q=encodeURIComponent(l?.name||'');try{let x=await supa('competitions?select=id,name,title&name=eq.'+q+'&limit=1');if(x[0]?.id)return x[0].id;x=await supa('competitions?select=id,name,title&title=eq.'+q+'&limit=1');return x[0]?.id||null}catch(e){return null}}
async function loadHomeStats(l){const h=document.getElementById('homeStats');if(!h)return;statsCss();h.innerHTML='<div class="empty">⏳ Inapakia takwimu za '+esc(l.name)+'...</div>';try{const cid=await findCompetitionId(l);if(!cid){showHomeStatsMessage('Ligi hii bado haijaunganishwa na data ya mechi kwenye Supabase.');return}const [matches,allStats,clubs]=await Promise.all([supa('matches?competition_id=eq.'+cid+'&select=id,home_team_id,away_team_id,home_score,away_score'),supa('match_statistics?select=*'),supa('clubs?select=id,name')]);const ids=new Set(matches.map(m=>String(m.id))),rows=allStats.filter(s=>ids.has(String(s.match_id)));if(!rows.length){showHomeStatsMessage('Hakuna rekodi za kina za takwimu kwa ligi hii bado.');return}const metrics=[['Possession','possession',1,true,'avg'],['Shots','shots',0,false,'sum'],['Shots on target','shots_on_target',0,false,'sum'],['Shots off target','shots_off_target',0,false,'sum'],['Blocked shots','blocked_shots',0,false,'sum'],['Corners','corners',0,false,'sum'],['Fouls','fouls',0,false,'sum'],['Offsides','offsides',0,false,'sum'],['Big chances','big_chances',0,false,'sum'],['xG','xg',2,false,'sum'],['Passes','passes',0,false,'sum'],['Pass accuracy','pass_accuracy',1,true,'avg'],['Saves','saves',0,false,'sum'],['Crosses','crosses',0,false,'sum'],['Duels won','duels_won',0,false,'sum'],['Momentum','momentum',1,false,'avg']];const cards=metrics.map(m=>statCard(m[0],statValue(rows,'home',m[1],m[4]),statValue(rows,'away',m[1],m[4]),m[2],m[3])).join('');const names=Object.fromEntries(clubs.map(x=>[String(x.id),x.name]));const team={};matches.forEach(m=>{const hId=String(m.home_team_id),aId=String(m.away_team_id);team[hId]??={id:hId,name:names[hId]||hId,played:0,goals:0};team[aId]??={id:aId,name:names[aId]||aId,played:0,goals:0};team[hId].played++;team[aId].played++;team[hId].goals+=Number(m.home_score)||0;team[aId].goals+=Number(m.away_score)||0});const statByTeam={};matches.forEach(m=>{const sh=rows.find(r=>String(r.match_id)===String(m.id));if(!sh)return;const hh=String(m.home_team_id),aa=String(m.away_team_id);statByTeam[hh]??={name:names[hh]||hh,shots:0,on:0,corners:0,xg:0,possession:0,passAcc:0,fouls:0,saves:0,big:0,duels:0,statGames:0};statByTeam[aa]??={name:names[aa]||aa,shots:0,on:0,corners:0,xg:0,possession:0,passAcc:0,fouls:0,saves:0,big:0,duels:0,statGames:0};const H=statByTeam[hh],A=statByTeam[aa];H.shots+=Number(sh.home_shots)||0;H.on+=Number(sh.home_shots_on_target)||0;H.corners+=Number(sh.home_corners)||0;H.xg+=Number(sh.home_xg)||0;H.possession+=Number(sh.home_possession)||0;H.passAcc+=Number(sh.home_pass_accuracy)||0;H.fouls+=Number(sh.home_fouls)||0;H.saves+=Number(sh.home_saves)||0;H.big+=Number(sh.home_big_chances)||0;H.duels+=Number(sh.home_duels_won)||0;H.statGames++;A.shots+=Number(sh.away_shots)||0;A.on+=Number(sh.away_shots_on_target)||0;A.corners+=Number(sh.away_corners)||0;A.xg+=Number(sh.away_xg)||0;A.possession+=Number(sh.away_possession)||0;A.passAcc+=Number(sh.away_pass_accuracy)||0;A.fouls+=Number(sh.away_fouls)||0;A.saves+=Number(sh.away_saves)||0;A.big+=Number(sh.away_big_chances)||0;A.duels+=Number(sh.away_duels_won)||0;A.statGames++});const trs=Object.values(team).sort((x,y)=>y.goals-x.goals||y.played-x.played).map((t,i)=>{const s=statByTeam[t.id]||{};return '<tr><td>'+(i+1)+'</td><td>'+esc(t.name)+'</td><td>'+t.played+'</td><td><b>'+t.goals+'</b></td><td>'+((s.shots??0))+'</td><td>'+((s.on??0))+'</td><td>'+((s.corners??0))+'</td><td>'+((s.xg??0).toFixed(2))+'</td><td>'+((s.statGames?s.possession/s.statGames:0).toFixed(1))+'%</td><td>'+((s.statGames?s.passAcc/s.statGames:0).toFixed(1))+'%</td><td>'+((s.fouls??0))+'</td><td>'+((s.saves??0))+'</td><td>'+((s.big??0))+'</td><td>'+((s.duels??0))+'</td></tr>'}).join('');const leaders=[['🔥 Shots','shots',v=>v],['🎯 Shots on Target','on',v=>v],['🚩 Corners','corners',v=>v],['📈 xG','xg',v=>Number(v).toFixed(2)],['🟢 Possession','possession',v=>((v.statGames?v.possession/v.statGames:0).toFixed(1))+'%'],['🎯 Pass Accuracy','passAcc',v=>((v.statGames?v.passAcc/v.statGames:0).toFixed(1))+'%'],['💥 Big Chances','big',v=>v],['🤝 Duels Won','duels',v=>v],['🛡️ Saves','saves',v=>v]];const leaderHtml=leaders.map(([label,key,fmt])=>{const arr=Object.values(statByTeam);const best=arr.sort((a,b)=>{const av=key==='possession'||key==='passAcc'?(a.statGames?a[key]/a.statGames:0):Number(a[key]||0);const bv=key==='possession'||key==='passAcc'?(b.statGames?b[key]/b.statGames:0):Number(b[key]||0);return bv-av})[0];return '<div><b>'+label+'</b><br>'+esc(best?.name||'—')+' — '+(best?fmt(key==='possession'||key==='passAcc'?(best.statGames?best[key]/best.statGames:0):best[key]||0):'—')+'</div>'}).join('');h.innerHTML='<p class="home-stat-note">Takwimu za <b>'+esc(l.name)+'</b> kutoka kwenye mechi '+matches.length+' zenye rekodi za statistics.</p><div class="home-stat-grid">'+cards+'</div><div class="home-stat-table"><table><thead><tr><th>#</th><th>Timu</th><th>MP</th><th>GF</th><th>Shots</th><th>SOT</th><th>Corners</th><th>xG</th><th>Poss.</th><th>Pass%</th><th>Fouls</th><th>Saves</th><th>Big Chances</th><th>Duels</th></tr></thead><tbody>'+trs+'</tbody></table></div><div class="home-stat-table"><h3>🏆 Leaders za Takwimu</h3><div class="leader-grid">'+leaderHtml+'</div></div>'}catch(e){console.error(e);showHomeStatsMessage('Takwimu hazikupatikana kwa sasa. Jaribu ku-refresh ukurasa.')}}

const modal=document.getElementById('modal');document.getElementById('newLeague').onclick=()=>{location.href='admin.html'};document.getElementById('close').onclick=()=>modal.classList.add('hidden');document.getElementById('save').onclick=async()=>{const name=document.getElementById('leagueName').value.trim(),season=document.getElementById('season').value.trim(),type=document.getElementById('competitionType').value,division=document.getElementById('leagueDivision').value;if(!name)return alert('Andika jina la ligi.');const save=document.getElementById('save');save.disabled=true;try{const existing=await supa('competitions?select=id,name,season&name=eq.'+encodeURIComponent(name)+'&season=eq.'+encodeURIComponent(season||'2026/2027')+'&limit=1');if(existing[0]){alert('Ligi hii tayari ipo kwenye mfumo online.');return}const created=await supa('competitions',{method:'POST',body:JSON.stringify({name,competition_type:type,organizer:'AFRN',season:season||'2026/2027',status:'ACTIVE'})});const leagues=getLeagues().filter(l=>String(l.name).toLowerCase()!==name.toLowerCase());leagues.push({name,type,division,season:season||'2026/2027',teams:[],source:'supabase',sourceCompetitionId:created[0]?.id});localStorage.setItem(leagueKey,JSON.stringify(leagues));modal.classList.add('hidden');document.getElementById('leagueName').value='';render();setupStatsSelector()}catch(e){alert('Ligi haijahifadhiwa: '+e.message)}finally{save.disabled=false}};

const teamModal=document.getElementById('teamModal');document.getElementById('newTeam').onclick=()=>{location.href='admin.html'};document.getElementById('teamClose').onclick=()=>teamModal.classList.add('hidden');document.getElementById('teamSave').onclick=async()=>{const jina=document.getElementById('teamName').value.trim(),division=document.getElementById('teamDivision').value,zone=document.getElementById('teamZone').value.trim();if(!jina)return alert('Andika jina la timu.');const save=document.getElementById('teamSave');save.disabled=true;try{const existing=await supa('clubs?select=id,name,division&name=eq.'+encodeURIComponent(jina)+'&division=eq.'+encodeURIComponent(division)+'&limit=1');if(existing[0]){alert('Timu hii tayari ipo online katika daraja hili.');return}const created=await supa('clubs',{method:'POST',body:JSON.stringify({name:jina,division,zone:zone||null,status:'active'})});if(!created[0])throw new Error('Supabase haikurudisha timu mpya.');const teams=getTeams().filter(t=>!(String(t.jina||t.name).toLowerCase()===jina.toLowerCase()&&t.division===division));teams.push({id:created[0].id,jina,division,zone,P:0,W:0,D:0,L:0,GF:0,GA:0,Pts:0,Red:0,Yellow:0});saveTeams(teams);teamModal.classList.add('hidden');document.getElementById('teamName').value='';document.getElementById('teamZone').value='';render();renderTeamPreview()}catch(e){alert('Timu haijahifadhiwa: '+e.message)}finally{save.disabled=false}};

async function supa(path,options={}){const r=await fetch(SUPABASE_URL+'/rest/v1/'+path,{...options,headers:{apikey:SUPABASE_KEY,Authorization:'Bearer '+SUPABASE_KEY,'Content-Type':'application/json',Prefer:options.method==='POST'?'return=representation':'return=minimal',...(options.headers||{})}});if(!r.ok){const t=await r.text();throw new Error('Supabase '+r.status+' '+t)}const t=await r.text();return t?JSON.parse(t):[]}
async function syncLeagues(){
  try{
    // Tumia columns salama tu hapa; column moja lisilokuwepo lisizuie ligi zote.
    const db=await supa('competitions?select=id,name,competition_type,season,status&order=name');
    const current=JSON.parse(localStorage.getItem(leagueKey)||'[]');
    const synced=[];
    for(const x of db){
      const old=current.find(l=>String(l.sourceCompetitionId||'')===String(x.id));
      let ct=[],matches=[];
      try{ct=await supa('competition_teams?select=club_id&competition_id=eq.'+encodeURIComponent(x.id)+'&limit=500')}catch(e){console.warn('competition_teams:',x.name,e)}
      try{matches=await supa('matches?select=id&competition_id=eq.'+encodeURIComponent(x.id)+'&limit=500')}catch(e){console.warn('matches:',x.name,e)}
      const kind=String(x.competition_type||'');
      const tournament=/tournament|cup|group\s*stage|knockout/i.test(kind)||/cup/i.test(String(x.name||''));
      synced.push({name:x.name,type:tournament?'Tournament':'League',division:old?.division||x.name,season:x.season||old?.season||'2026/2027',teams:old?.teams||[],source:'supabase',sourceCompetitionId:x.id,teamCount:new Set(ct.map(r=>String(r.club_id)).filter(Boolean)).size,matchCount:matches.length,competitionType:kind});
    }
    // Hakikisha ligi/mashindano ya msingi yanaonekana hata kama query ya count haikupata rows.
    const known=[
      ['UPENDO WA WAKIMBIZI CUP','3b55884e-05bc-4d83-832e-3addcd97d970','Tournament',24,50,'2026'],
      ['MULUMBA PREMIER LEAGUE','9f077f46-74e1-49a5-998c-0cdf9fd2efd3','League',18,6,'2026']
    ];
    known.forEach(([name,id,type,teams,matches,season])=>{
      const i=synced.findIndex(x=>String(x.sourceCompetitionId)===id);
      if(i<0)synced.push({name,type,division:name,season,teams:[],source:'supabase',sourceCompetitionId:id,teamCount:teams,matchCount:matches,competitionType:type});
      else if(!synced[i].teamCount)synced[i].teamCount=teams;
      else if(!synced[i].matchCount)synced[i].matchCount=matches;
    });
    // Ligi Kuu ibaki kama mfumo mmoja wenye selector ya madaraja.
    if(!synced.some(x=>/^ligi kuu$/i.test(String(x.name||'')))){
      synced.unshift({name:'Ligi Kuu',type:'League',division:'Ligi Kuu',season:'2026/2027',teams:[],source:'supabase',sourceCompetitionId:'82371a79-221e-4085-b9c1-171f93739a1e',teamCount:0,matchCount:0});
    }
    localStorage.setItem(leagueKey,JSON.stringify(synced));
    render();setupStatsSelector();return synced;
  }catch(e){
    console.error('League sync failed:',e);
    const fallback=[
      {name:'Ligi Kuu',type:'League',division:'Ligi Kuu',season:'2026/2027',teams:[],source:'supabase',sourceCompetitionId:'82371a79-221e-4085-b9c1-171f93739a1e',teamCount:0,matchCount:0},
      {name:'UPENDO WA WAKIMBIZI CUP',type:'Tournament',division:'Daraja la I',season:'2026',teams:[],source:'supabase',sourceCompetitionId:'3b55884e-05bc-4d83-832e-3addcd97d970',teamCount:24,matchCount:50,competitionType:'Tournament'},
      {name:'MULUMBA PREMIER LEAGUE',type:'League',division:'MULUMBA PREMIER LEAGUE',season:'2026',teams:[],source:'supabase',sourceCompetitionId:'9f077f46-74e1-49a5-998c-0cdf9fd2efd3',teamCount:18,matchCount:6,competitionType:'League'}
    ];
    localStorage.setItem(leagueKey,JSON.stringify(fallback));render();setupStatsSelector();return fallback;
  }
}
function localDate(offset){const d=new Date();d.setDate(d.getDate()+offset);return d.toLocaleDateString('en-CA',{timeZone:'Africa/Dar_es_Salaam'})}
const notificationStateKey='afrn_notification_match_state';
function notificationPrefs(){return{enabled:localStorage.getItem('afrn_notifications_enabled')!=='0',goals:localStorage.getItem('afrn_goal_alerts')!=='0',starts:localStorage.getItem('afrn_match_start_alerts')!=='0'}}
function notifyFootball(title,body,url){if(!('Notification'in window)||Notification.permission!=='granted')return;try{const n=new Notification(title,{body,tag:'afrn-football'});n.onclick=()=>{try{window.focus();if(url)location.href=url}catch(e){}}}catch(e){}}
async function checkFootballNotifications(){const p=notificationPrefs();if(!p.enabled||(!p.goals&&!p.starts)||!('Notification'in window)||Notification.permission!=='granted')return;try{const rows=await supa('matches?select=id,competition_id,home_team_id,away_team_id,match_date,match_time,home_score,away_score,status,venue&order=match_date,match_time');const clubs=await supa('clubs?select=id,name,short_name');const names=Object.fromEntries(clubs.map(c=>[String(c.id),c.name||c.short_name||'Timu']));const followed=new Set([...getFollowed(),...getFollowedCompetitions()].map(String));let state={};try{state=JSON.parse(localStorage.getItem(notificationStateKey)||'{}')}catch(e){};const next={};rows.forEach(m=>{const id=String(m.id),h=String(m.home_team_id),a=String(m.away_team_id),key=m.competition_id?String(m.competition_id):'',followedMatch=followed.has(h)||followed.has(a)||followed.has(key),hs=m.home_score==null?null:Number(m.home_score),as=m.away_score==null?null:Number(m.away_score),prev=state[id]||{};next[id]={home:hs,away:as,status:String(m.status||'').toLowerCase()};if(!followedMatch)return;const hn=names[h]||'Nyumbani',an=names[a]||'Ugenini';if(p.goals&&prev.home!=null&&hs!=null&&hs>prev.home)notifyFootball('⚽ Goli — '+hn,hn+' '+hs+' - '+as+' '+an,'match.html?id='+encodeURIComponent(id));if(p.goals&&prev.away!=null&&as!=null&&as>prev.away)notifyFootball('⚽ Goli — '+an,hn+' '+hs+' - '+as+' '+an,'match.html?id='+encodeURIComponent(id));const live=['live','in_progress','playing','1h','2h'].includes(String(m.status||'').toLowerCase());if(p.starts&&live&&!['live','in_progress','playing','1h','2h'].includes(String(prev.status||'')))notifyFootball('🔴 Mechi imeanza',hn+' vs '+an,'match.html?id='+encodeURIComponent(id))});localStorage.setItem(notificationStateKey,JSON.stringify(next))}catch(e){console.warn('Notification check failed',e)}}

// PWA install
let deferredInstallPrompt=null;
function setupPwaInstall(){
  const btn=document.getElementById('installApp');if(!btn)return;
  const standalone=window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone===true;
  if(standalone){btn.hidden=true;return}
  window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredInstallPrompt=e;btn.hidden=false});
  btn.addEventListener('click',async()=>{
    if(deferredInstallPrompt){
      deferredInstallPrompt.prompt();
      try{await deferredInstallPrompt.userChoice}catch(e){}
      deferredInstallPrompt=null;btn.hidden=true;
    }else{
      alert('Android/Chrome: tumia menyu ya Chrome → Install app au Add to Home screen. iPhone: Share → Add to Home Screen.');
    }
  });
  window.addEventListener('appinstalled',()=>{btn.hidden=true;deferredInstallPrompt=null});
}

document.querySelectorAll('.quick').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.quick').forEach(x=>x.classList.remove('active'));b.classList.add('active');loadMatchFeed(b.dataset.view)}));
function updateAccountNav(){const n=document.getElementById('accountNav');if(!n)return;try{const s=JSON.parse(localStorage.getItem('afrn_auth_session')||'null');if(s?.access_token){const name=s.user?.user_metadata?.full_name||s.user?.email?.split('@')[0]||'Akaunti';n.textContent='👤 '+name;n.href='auth.html'}else{n.textContent='👤 Login';n.href='auth.html'}}catch(e){n.textContent='👤 Login';n.href='auth.html'}}
setupPwaInstall();
try{if(localStorage.getItem('afrn_app_build')!==APP_BUILD){localStorage.setItem('afrn_app_build',APP_BUILD);localStorage.removeItem(leagueKey)}}catch(e){}
render();renderTeamPreview();setupStatsSelector();setupFollowPicker();updateAccountNav();loadCloudPreferences().then(()=>{renderFollowedPreview();renderFollowList()});loadFollowPicker();if(!localStorage.getItem(favouritesOnboardedKey)&&!getFollowedCompetitions().length&&!getFollowed().length&&!getFollowedPlayers().length){setTimeout(()=>document.getElementById('followModal')?.classList.remove('hidden'),700)}loadMatchFeed('today');loadLiveStrip();syncLeagues().then(()=>{renderTeamPreview();setupStatsSelector()});setTimeout(checkFootballNotifications,4000);setInterval(()=>{loadLiveStrip();checkFootballNotifications()},15000);
async function loadFollowPicker(){const list=document.getElementById('followList');if(!list)return;list.innerHTML='<div class="empty">⏳ Inapakia...</div>';try{if(!followCache.competitions.length){const ls=getLeagues();followCache.competitions=ls.map((x,i)=>({...x,id:String(x.sourceCompetitionId||('local-'+i)),_index:i}));}if(!followCache.teams.length)followCache.teams=await supa('clubs?select=id,name,short_name,division,zone,logo_url&order=name&limit=300');if(!followCache.players.length)followCache.players=await supa('players?select=id,first_name,middle_name,last_name,jersey_number,position,photo_url,club_id&order=last_name,first_name&limit=500');renderFollowList();renderFollowedPreview()}catch(e){list.innerHTML='<div class="empty">Data haikupatikana kwa sasa.</div>'}}
function followPlayerName(p){return [p.first_name,p.middle_name,p.last_name].filter(Boolean).join(' ')||'Mchezaji'}
function renderFollowList(){const list=document.getElementById('followList'),q=(document.getElementById('followSearch')?.value||'').trim().toLowerCase();if(!list)return;const source=followTab==='competitions'?followCache.competitions:(followTab==='teams'?followCache.teams:followCache.players);const rows=source.filter(x=>{const n=followTab==='competitions'?(x.name||''):(followTab==='teams'?(x.name||x.short_name||''):(followPlayerName(x)));return n.toLowerCase().includes(q)}).slice(0,100);const followed=followTab==='competitions'?getFollowedCompetitions():(followTab==='teams'?getFollowed():getFollowedPlayers());if(!rows.length){list.innerHTML='<div class="empty">Hakuna matokeo.</div>';return}list.innerHTML=rows.map(x=>{const id=String(x.id),yes=followed.includes(id),name=followTab==='teams'?(x.name||x.short_name||'Timu'):followPlayerName(x),meta=followTab==='teams'?[x.division,x.zone].filter(Boolean).join(' • '):(x.position||'Mchezaji');const img=followTab==='teams'?x.logo_url:x.photo_url;return '<button type="button" class="follow-row '+(yes?'selected':'')+'" data-follow-id="'+esc(id)+'">'+(img?'<img src="'+esc(img)+'" alt="">':'<span class="follow-avatar">'+(followTab==='competitions'?'🏆':(followTab==='teams'?'⚽':'👤'))+'</span>')+'<span><b>'+esc(name)+'</b><small>'+esc(meta||'—')+'</small></span><strong>'+(yes?'✓':'＋')+'</strong></button>'}).join('');list.querySelectorAll('.follow-row').forEach(btn=>btn.onclick=()=>{followTab==='competitions'?toggleFollowCompetition(btn.dataset.followId):(followTab==='teams'?toggleFollow(btn.dataset.followId):toggleFollowPlayer(btn.dataset.followId))})}
function renderFollowedPreview(){const box=document.getElementById('followedPreview');if(!box)return;const fc=getFollowedCompetitions(),ft=getFollowed(),fp=getFollowedPlayers(),comps=followCache.competitions.filter(x=>fc.includes(String(x.id))),teams=followCache.teams.filter(x=>ft.includes(String(x.id))),players=followCache.players.filter(x=>fp.includes(String(x.id)));if(!fc.length&&!ft.length&&!fp.length){box.innerHTML='<div class="empty">Bado hujachagua timu au wachezaji. Bonyeza ⭐ Chagua.</div>';return}box.innerHTML='<div class="followed-chips">'+comps.map(x=>'<a href="competition.html?id='+encodeURIComponent(x.id)+'" class="follow-chip">🏆<span>'+esc(x.name||'Ligi/Mashindano')+'</span></a>').join('')+teams.map(x=>'<a href="team.html?id='+encodeURIComponent(x.id)+'" class="follow-chip">'+(x.logo_url?'<img src="'+esc(x.logo_url)+'" alt="">':'⚽')+'<span>'+esc(x.name||x.short_name)+'</span></a>').join('')+players.map(x=>'<a href="player.html?id='+encodeURIComponent(x.id)+'" class="follow-chip">'+(x.photo_url?'<img src="'+esc(x.photo_url)+'" alt="">':'👤')+'<span>'+esc(followPlayerName(x))+'</span></a>').join('')+'</div>'}
function setupFollowPicker(){const modal=document.getElementById('followModal');if(!modal)return;document.getElementById('editFavourites').onclick=()=>{modal.classList.remove('hidden');loadFollowPicker()};document.getElementById('followClose').onclick=()=>modal.classList.add('hidden');document.getElementById('followDone').onclick=()=>{localStorage.setItem(favouritesOnboardedKey,'1');modal.classList.add('hidden')};document.querySelectorAll('.follow-tab').forEach(b=>b.onclick=()=>{followTab=b.dataset.followTab;document.querySelectorAll('.follow-tab').forEach(x=>x.classList.toggle('active',x===b));document.getElementById('followSearch').value='';renderFollowList()});document.getElementById('followSearch').addEventListener('input',renderFollowList);renderFollowedPreview()}
