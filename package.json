{
  "name": "afrn-league-management",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "android:build": "npm run prepare:android && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "@capacitor/android": "^7.4.3"
  }
}